<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.mityushaa.alarmtdd.AlarmActivity">

    <Button android:id="@+id/btStart" android:layout_height="100dp"
        android:layout_width="120dp"
        android:onClick="startRepeatingTimer"
        tools:context=".WidgetAlarmManagerActivity"
        android:textColor="#ffffff"
        android:background="@drawable/alarm"
        android:layout_alignParentTop="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="44dp" />

    <Button android:id="@+id/btOneTime" android:layout_height="wrap_content"
        android:layout_width="match_parent"
        android:text="@string/one_time"
        tools:context=".WidgetAlarmManagerActivity"
        android:onClick="onetimeTimer"
        android:background="@color/colorPrimaryDark"
        android:textColor="#ffffff"
        android:layout_below="@+id/btStart"
        android:layout_alignParentStart="true"
        android:layout_marginTop="39dp" />

    <Button android:id="@+id/btCancel" android:layout_height="100dp"
        android:layout_width="100dp"
        tools:context=".WidgetAlarmManagerActivity"
        android:onClick="cancelRepeatingTimer"
        android:textColor="#ffffff"
        android:background="@android:drawable/ic_menu_close_clear_cancel"
        android:layout_alignTop="@+id/btStart"
        android:layout_alignEnd="@+id/btOneTime" />

</RelativeLayout>
